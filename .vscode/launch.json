{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Website lokal testen",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:8000",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "Server starten & Cache leeren",
      "userDataDir": "${workspaceFolder}/.vscode/.chrome-debug-profile"
    }
  ]
}
